# JWT Secret (must match backend JWT_SECRET)
# This secret is used to verify JWT tokens issued by the backend
# IMPORTANT: Must be the same value as JWT_SECRET in backend .env file
JWT_SECRET=your-jwt-secret-key-here

# Backend URL for post-scrape hooks (census backfill). Optional.
# If set, after each completed scrape the scraper calls POST /api/census/backfill-properties
# to backfill census_tract_geoid and census_tract_class_* for properties missing them.
# Auth uses JWT signed with JWT_SECRET (aud: census-backfill)—no extra key needed.
# BACKEND_URL=https://your-backend.railway.app

# CORS Origins (comma-separated, use * for all origins)
# Include every origin that will call the scraper API (frontend production, Vercel, preview URLs, etc.).
# Example: https://rei.engreg.com,https://real-estate-crm-frontend-one.vercel.app
# For development, you can use * to allow all origins
CORS_ORIGINS=*

# MongoDB Configuration
MONGODB_URL=mongodb://localhost:27017
DATABASE_NAME=mls_scraper

# API Configuration
API_HOST=0.0.0.0
API_PORT=8000

# Scraping Configuration
MAX_CONCURRENT_JOBS=5
REQUEST_DELAY=1.0
MAX_RETRIES=3
RATE_LIMIT_PER_MINUTE=60

# DataImpulse Proxy Configuration (Optional)
# Set USE_DATAIMPULSE=false to disable DataImpulse proxy even if credentials are provided
USE_DATAIMPULSE=true
DATAIMPULSE_LOGIN=your_dataimpulse_login_here
DATAIMPULSE_PASSWORD=your_dataimpulse_password_here
DATAIMPULSE_ENDPOINT=gw.dataimpulse.com:823

# Logging
LOG_LEVEL=INFO
