fastapi>=0.115.0
uvicorn>=0.30.0
# Install curl-cffi FIRST before homeharvest to ensure it's available when homeharvest imports it
curl-cffi>=0.5.10  # Required for TLS fingerprinting to bypass Realtor.com anti-bot detection
# homeharvest==0.8.11  # Original version (doesn't support curl_cffi)
# Using forked version with curl_cffi support for TLS fingerprinting
git+https://github.com/ezraedl/HomeHarvestLocal.git@master
# homeharvest>=0.8.17  # Official version (doesn't have USE_CURL_CFFI export)
pymongo==4.6.0
motor==3.3.2
pydantic>=2.11.7,<3.0.0
python-multipart==0.0.6
python-dotenv==1.0.0
pandas>=2.3.1,<3.0.0
aiofiles==23.2.1
httpx>=0.27.0
croniter==1.4.1
pyyaml==6.0.1

PyJWT>=2.8.0

# OpenAI Agent Lookup library (local package)
# Install the local package so it's available for imports
# Dependencies will be installed automatically from setup.py/pyproject.toml
./openai_agent_lookup
# Explicitly include key dependencies to ensure they're installed
openai-agents>=0.6.0
openai>=2.9.0
nest-asyncio>=1.5.0