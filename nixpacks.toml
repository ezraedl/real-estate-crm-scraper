# Nixpacks config for Railway. Ensures Playwright Chromium is installed for Rentcast rent-estimation.

[phases.install]
# "..." keeps the default (e.g. pip install -r requirements.txt); we append Playwright browser + system deps
cmds = ["...", "playwright install --with-deps chromium"]
